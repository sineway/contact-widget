/**
 * @template T
 * @typedef {import('ol').Collection<T>} Collection
*/

/**
 * @typedef {import('ol').Feature} Feature
 * @typedef {import('ol').MapBrowserEvent} MapBrowserEvent
 * @typedef {import('ol/interaction').Interaction} Interaction
 */

/**
 * @typedef Contact
 * @prop {number} id
 * @prop {string} address
 * @prop {string[]} budgets
 * @prop {number} latitude
 * @prop {number} longitude
 */
